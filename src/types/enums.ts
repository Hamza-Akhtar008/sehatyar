export enum AvailabilityType {
  CLINIC = 'clinic',
  ONLINE = 'online',
}

export enum SlotType {
  MORNING = 'morning',
  AFTERNOON = 'afternoon',
  EVENING = 'evening',
}

export enum UserRole {
  ADMIN = 'admin',
  DOCTOR = 'doctor',
  clinic='clinic',
  RECEPTIONIST = 'receptionist',
  PATIENT = 'patient',
}